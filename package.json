{
  "name": "the-grid",
  "version": "1.0.0",
  "description": "",
  "main": "grid.js",
  "scripts": {
    "build": "npm install -y",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^6.3.0"
  }
}
